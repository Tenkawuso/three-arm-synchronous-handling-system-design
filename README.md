# Three Arm Synchronous Handling System Design

ROS1 + MoveIt + Docker-based simulation for a three-arm collaborative handling project.

## Quick Start

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\run_noetic_smoke.ps1
```

## Details

See `RUN_DOCKER_NOETIC_WIN10.md` for the Windows 10 setup and simulation workflow.
